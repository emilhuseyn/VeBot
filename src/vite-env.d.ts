/// <reference types="vite/client" />

// Fontsource variable packages ship CSS only (no type declarations);
// declare them so side-effect imports typecheck under strict mode.
declare module "@fontsource-variable/inter";
