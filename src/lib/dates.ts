import type { Locale } from "@/lib/types";

/** BCP 47 tags for Intl formatters and the document `lang` attribute. */
export const LOCALE_TAGS: Record<Locale, string> = {
  az: "az-Latn-AZ",
  en: "en-GB",
  ru: "ru-RU",
};
